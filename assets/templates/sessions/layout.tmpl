{{define "Head"}}
    <link rel="stylesheet" type="text/css" href="/css/sessions.css">
{{end}}

{{define "Header"}} {{end}}

{{define "Body"}}
<div class="sessions-container">
    <div class="element">
        <h1 class="title">
        {{template "Title" $}}
        </h1>
    </div>
    <div class="element">
    {{template "Form" $}}
    </div>
</div>
{{end}}

{{define "Footer"}} {{end}}
